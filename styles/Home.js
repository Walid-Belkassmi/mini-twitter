import { StyleSheet } from "react-native";

const homeStyle = StyleSheet.create({
  container: {
    margin: 12,
  },
  input: {
    borderWidth: 1,
    padding: 10,
    height: 100,
  },
});

export default homeStyle;
